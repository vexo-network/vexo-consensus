# مواصفة الإجماع

> Locale: ar · العربية
> هذا المستند ترجمة عربية مباشرة للمصدر الإنجليزي. تبقى قرارات البروتوكول والأمان والإصدار معيارية في النص الإنجليزي.


## ترتيب القراءة

تشرح هذه الوثيقة المواصفة المعيارية لـ Consensus Spec. إذا كانت هذه أول قراءة لك، فاتبع هذا الترتيب.

1. Scope
2. Roles
3. State
4. Message Types
5. Safety Rules
6. Finality Rule
7. Execution Commit Policy
8. Liveness Assumptions
9. Empty Blocks and Round Recovery
10. Evidence

هذا الترتيب يطابق طريقة القراءة الصحيحة: أولاً النطاق والحالة، ثم قواعد الرسائل والسلامة والحيوية، وفي النهاية الأدلة.

## نظرة عامة

يساعد هذا المستند على فهم المواصفة المعيارية لـ state machine الإجماع وربط ذلك بقرارات التنفيذ والتشغيل.

- Canonical path: `docs/specs/consensus-spec.md`
- Locale path: `docs/locales/ar/specs/consensus-spec.md`

## لماذا تقرأ هذا المستند

- المواصفة المعيارية لـ state machine الإجماع
- تحقق أولاً من عبارات MUST/SHOULD/MAY في المصدر الإنجليزي.
- هذا المستند المحلي يساعد على الفهم؛ قرارات audit وrelease وsecurity تُحسم بالمصدر الإنجليزي.

## ما الذي يجب أن تستطيع فعله بعد القراءة

- شرح قرار التنفيذ أو التشغيل الذي يدعمه هذا المستند.
- ربط المتطلبات المعيارية في المصدر الإنجليزي بإعدادات الشبكة الحالية.
- التحقق من chain ID وvalidator ID وfee/gas وعناوين peer قبل نسخ الأمثلة.

## قائمة تحقق للاستخدام الآمن

- تحقق أولاً من عبارات MUST/SHOULD/MAY في المصدر الإنجليزي.
- لا تترجم الأوامر أو config key أو أسماء RPC أو حقول JSON أو معرّفات الكود.
- قبل نسخ الأمثلة، طابق chain ID وvalidator ID وfee/gas وعناوين peer مع شبكتك.
- بعد تعديل الوثائق، شغّل `make docs-check` للتحقق من شجرة المستندات المحلية وحراس الترجمة.

## نقاط يجب الانتباه لها

- هذا المستند المحلي يساعد على الفهم؛ قرارات audit وrelease وsecurity تُحسم بالمصدر الإنجليزي.
- عند تغير التنفيذ، حدّث المصدر الإنجليزي وكل المستندات المحلية في نفس التغيير.

## واجهات يجب الحفاظ عليها كما هي

- `(height, round)`
- `chain_id`
- `height`
- `round`
- `phase`
- `validator_set_hash`
- `locked_qc`
- `high_qc`
- `last_timeout_cert`
- `last_finalized`
- `Proposal`
- `Vote`
- `TimeoutVote`
- `QuorumCert`
- `TimeoutCert`
- `>= 2/3`
- `B3`
- `B2`
- `B1`
- `B3.height = B2.height + 1`
- `B2.height = B1.height + 1`
- `execution_commit = "qc"`

## بنية المصدر الإنجليزي

- Consensus Spec
- Scope
- Roles
- State
- Message Types
- Safety Rules
- Finality Rule
- Execution Commit Policy
- Liveness Assumptions
- Evidence

## المصدر المعتمد

- [الوثيقة الإنجليزية المرجعية](../../en/specs/consensus-spec.md)
<!-- vexo-docs-ops-update-2026-06 -->

## الكتل الفارغة واستعادة Round

مع `create_empty_blocks=false` فإن ثبات height عندما يكون mempool فارغًا هو حالة idle طبيعية. عند وصول معاملة لا يقترح العقدة إلا إذا كانت proposer الحتمية للزوج `(height, round)` الحالي، ولا تقفز محليًا إلى round آخر. لا تتقدم round إلا عبر timeout certificate صالح أو انتقال finality معتمد، ولا تُعامل أخطاء التنفيذ والتخزين كـ timeout.

## مهلة الجولة المتكيفة

عندما تكون `adaptive_round_timeout_enabled = true` تحسب العقدة timeout من القيمة الأساسية وp95 المتحرك لمعالجة proposal/vote/commit ونتيجة التقدم وعجز peers النشطة. تضرب المهلة الميزانية في 1.5، ويطبق التقدم الناجح 0.8، وتضيف المدة المقاسة هامش أمان بثلاثة أضعاف، مع تقييد النتيجة بين الأساس وثمانية أمثاله. لا تتغير safe-vote أو proposer أو quorum power أو QC أو three-chain finality.

## بوابة نهائية الاسترداد

عندما تكون `recovery_finality_gate_enabled = true` وتختلف ارتفاعات application state وblock index الدائمة، يصبح أصغرهما safe recovery height. تؤجل finalized application commits الأعلى حتى تستعاد المطابقة. تعرض `/v1/metrics` و`/metrics/text` المهلة الحالية وتأجيلات الاسترداد.

## الترتيب الحتمي للمعاملات

تشتق proposal قيمة salt من `chain_id` وheight، وترتب transaction chain لكل signer حسب nonce تصاعديا، ثم تدمج الرؤوس وفق salted transaction hash. يزيل ذلك ترتيب وصول mempool المحلي لنفس candidate set، لكنه لا يضمن first-seen fairness أو censorship resistance أو confidentiality أو order fairness رسمية.

<!-- vexo-docs:technical-parity -->
## ملحق التكافؤ التقني

يساعد هذا الملحق على ضمان أن الترجمة تحتفظ بالواجهات القابلة للتنفيذ والأقسام الأساسية من الوثيقة الإنجليزية المعتمدة. تبقى الأوامر ومفاتيح الإعداد وطرق RPC وأسماء الحزم كما هي في كل اللغات.

### تتبع الأقسام
- section: Scope — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Roles — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: State — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Message Types — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Safety Rules — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Finality Rule — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Execution Commit Policy — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Liveness Assumptions — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Empty Blocks and Round Recovery — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.
- section: Evidence — يجب قراءة هذا القسم مع قيم الإعداد وأدلة التحقق وشروط الفشل والإجراءات المطلوبة من المشغل.

### واجهات تبقى دون تغيير
- `chain_id` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `validator_set_hash` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `locked_qc` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `high_qc` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `last_timeout_cert` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `last_finalized` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `>= 2/3` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `B3.height = B2.height + 1` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `B2.height = B1.height + 1` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `execution_commit = "qc"` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `execution_commit = "finalized"` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `block_committed` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `create_empty_blocks = false` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `latest_height = 0` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `latest_height` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `actual_hash` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `actual_time_unix_nano` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
- `parity_shards` — يُستخدم هذا الاسم كما هو في أمثلة التشغيل والتحقق من الإعداد، لذلك لا يُترجم.
