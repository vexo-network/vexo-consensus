services:
  validator-1:
    image: ${VEXO_IMAGE:-vexo-consensus:cgo}
    build: &vexo-cgo-build
      context: ../..
      dockerfile: Dockerfile.cgo
      args:
        GO_VERSION: ${VEXO_GO_VERSION:-1.26.5}
        VERSION: ${VERSION:-dev}
        COMMIT: ${COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}

  validator-2:
    image: ${VEXO_IMAGE:-vexo-consensus:cgo}
    build: *vexo-cgo-build

  validator-3:
    image: ${VEXO_IMAGE:-vexo-consensus:cgo}
    build: *vexo-cgo-build

  validator-4:
    image: ${VEXO_IMAGE:-vexo-consensus:cgo}
    build: *vexo-cgo-build
