name: vexo-single-init

services:
  init:
    image: ${VEXO_IMAGE:-vexo-consensus:dev}
    command:
      - network
      - init
      - --home
      - /var/lib/vexo/network
      - --chain-id
      - ${VEXO_CHAIN_ID:-vexo-chain}
      - --validators
      - ${VEXO_VALIDATORS:-4}
      - --p2p-base-port
      - "26656"
      - --rpc-base-port
      - "26657"
      - --p2p-port-step
      - "0"
      - --rpc-port-step
      - "0"
      - --p2p-host-template
      - validator-%d
      - --rpc-host-template
      - validator-%d
      - --p2p-listen-host
      - 0.0.0.0
      - --rpc-listen-host
      - 0.0.0.0
      - --overwrite
    volumes:
      - vexo-single-data:/var/lib/vexo

volumes:
  vexo-single-data:
    name: vexo-single-data
